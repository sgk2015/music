版本3.0：
    当前遇到的困难：原来设想的动态选择播放路径实现遇到了困难，初始化了播放路径，但是并不能
将动态选择的路径作为目标播放路径，而且自动播放下一首会出现没有绝对路径而异常退出
    替换方案：选定一个绝对路径作为应用默认路径，可以打开播放列表任意点击播放
              可以将打开文件选择的歌曲添加到默认播放路径，下次在默认播放路径直接进行播放
额外功能：指定音乐的歌词文件路径，在播放音乐的同时动态显示歌词